{
  "id": "VULNREPORT-TRANSFORMER-OSV",
  "meta": {
    "description": "Verifies the vulnreport transformer emits an OSV predicate by default."
  },
  "transformers": [
    { "id": "internal:vulnreport" }
  ],
  "tenets": [
    {
      "id": "osv-results-present",
      "predicates": {
        "types": ["https://ossf.github.io/osv-schema/results@v1.6.7"]
      },
      "code": "size(predicates) == 1 && size(predicates[0].data.results) > 0 && predicates[0].data.results[0].packages.exists(p, p.vulnerabilities.exists(v, v.id == 'CVE-2025-21613'))",
      "assessment": { "message": "OSV report contains the expected vulnerability" },
      "error": {
        "message": "OSV report did not match expectations",
        "guidance": "The vulnreport transformer should emit an OSV predicate listing CVE-2025-21613"
      }
    }
  ]
}
